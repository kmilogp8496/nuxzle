export default defineEventHandler(async (event) => {
//   const req = event.node.req
//   const authCookieName = useRuntimeConfig().authCookieName

//   console.info(`${req.method} ${req.url} ${getCookie(event, authCookieName)}`)
})
